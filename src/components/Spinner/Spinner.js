import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="loader" data-testid="spinner">
      <h1>Loading ...</h1>
    </div>
  );
};

export default Spinner;
